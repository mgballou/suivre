# Suivre — Decision Log

Running record of major decisions made while brainstorming the rebuild. Newest decisions appended at the bottom. Each entry: the decision, the reasoning, and what it rules out. This log feeds the final design spec.

> Context: reviving a personal food-and-symptom journal, previously a mostly-stubbed React/Vite + Hono/Drizzle monorepo (`../suivre-app`). Rebuilding on Laravel, drawing on the team's `architectural-sensibility.md` and the old app's data-model thinking.

---

## D1 — Reboot on Laravel; keep the domain sensibility, drop "Filament is the entire UI"

- **Decision:** Rebuild as a fresh Laravel app. Retain the *entire* domain layer from `architectural-sensibility.md` (invokable Actions carry all business logic, enums as domain primitives, domain events + observers + queued listeners, policies returning `Response`, strict Eloquent, PHPStan level 9, `declare(strict_types=1)`). Set aside **only** the rule "Filament is the entire authenticated UI."
- **Why:** Those domain patterns are stack-agnostic and are the "same team" test. The Filament-as-UI rule is the one part that doesn't fit a phone-first daily journal.
- **Rules out:** Continuing the old React/Hono monorepo; treating this as a pure Filament admin app.

## D2 — North star: correlation insights; logging serves correlatable signal

- **Decision:** The MVP's defining capability is **correlation insights** (surfacing which foods/triggers precede flare-ups/mood dips). Logging is designed *in service of* producing clean, correlatable signal — low-friction enough that data actually accumulates, structured enough to find patterns.
- **Why:** The whole point of the app is the food↔flare relationship. But insights need data, so logging friction is the enemy and must stay low.
- **Rules out:** Building elaborate logging/data-richness for its own sake before there's an insight to justify it.

## D3 — Time-lag correlation model (not naive same-timestamp)

- **Decision:** The correlation engine must model **time lag** — a trigger on day D may surface as a flare hours-to-days later. The old spec's "meal at time T vs symptom at time T" is explicitly rejected.
- **Why:** Inflammatory flare-ups and mood shifts lag their triggers. Same-timestamp correlation would be mostly noise.
- **Rules out:** Correlating only within a single day/timestamp.

## D4 — Tracked variables

- **Decision:** MVP inputs/triggers: **food & drink, stress, sleep.** Outcomes: **flare-ups (configurable conditions) + mood.** Meds/supplements **deferred** (same "logged event" shape, easy to add later).
- **Why:** Honest correlation needs the major confounders (sleep, stress) visible, not just food. Meds add scope without being day-one essential.
- **Rules out:** Nothing permanently — deferred items reuse the event model.

## D5 — Day-centric journal with a calendar front door

- **Decision:** The **day** is the central object; a **calendar is the primary UI** (period/fertility-tracker paradigm — Clue/Flo style). Any day can carry many tagged events or none.
  - **Baked-in daily check-in (fast, two-tap):** sleep (simple good/bad or 3-point scale) and mood (☹️ / 😐 / 🙂 — 3-point).
  - **Configurable conditions:** user-defined (e.g. eczema, joint pain, gut, brain fog), each logged per day with an intensity (0–10) and possibly other values.
  - **Food & acute flares:** events attached to a day.
- **Why:** Matches how these symptoms are lived and how proven tracker apps work; day-level time-series is ideal for lag correlation.
- **Rules out:** A CRUD-list-first admin paradigm as the primary user experience.

## D6 — Multi-user eventually; user-scoped from day one

- **Decision:** Scope every table by user and use real auth from the start. MVP is two users (you + partner), each a fully private journal. No public onboarding/marketing yet, but no architecture that blocks it.
- **Why:** Cheap to scope by user now; expensive to retrofit multi-tenancy later.
- **Rules out:** Single-global-user shortcuts; building public signup/onboarding now.

## D7 — Frontend: Livewire/Volt bespoke PWA for users; Filament as operator backstage

- **Decision:** Build the user-facing journal as a **bespoke Livewire/Volt + Tailwind + Alpine PWA** (home-screen installable, online-first for MVP). Keep **Filament as an internal operator backstage**: data oversight, reference-data management, runtime settings (incl. correlation parameters via Spatie Settings), and near-free internal dashboards. Two audiences, two UIs, one shared domain layer.
- **Why:** One language/mental model, same engine as Filament (cohesion), least tooling for essentially one maintainer, matches the team's server-driven style. React/Inertia's main edge (rich offline) isn't needed now.
- **Offline:** Explicitly out of scope for MVP (online-first). Can be added later if missed.
- **Rules out:** Inertia+React, Inertia+Vue (Vue eliminated by preference), and native mobile development.

## D8 — Charting standard + visualization approach

- **Decision:** Standardize on **one charting library** used across both Filament widgets and the bespoke app (candidate: **ApexCharts** — Filament's default — or **Chart.js**; final pick TBD). Add a dedicated **calendar-heatmap** component for the period-tracker-style heatmap view. Govern all visuals with the **`dataviz` skill** (one coherent, accessible, light/dark system) and use the **`frontend-design` skill** for the bespoke UI polish. Lean on Filament widgets for internal dashboards.
- **Why:** Insights/visualizations are a priority and the maintainer is "no frontend guru" — vetted, themeable, AI-friendly libraries + a design-system skill keep us out of hand-rolled D3.
- **Rules out:** Per-view ad-hoc charting libraries; bespoke low-level D3 as the default.

## D9 — Curated taxonomy + deterministic classifier; AI is NOT in the MVP

- **Decision:** Trigger tags come from a **curated, research-based taxonomy** (operator-owned reference data, managed in Filament, global/shared, stable) — not user-invented and not AI-generated. Food entries are classified by a **deterministic classifier**: normalize text → resolve synonyms/aliases → fuzzy-match (e.g. Postgres `pg_trgm` trigram similarity) against a curated **food/dish → category knowledge base**, with curated compositions for common composite dishes. Unmatched/low-confidence entries go to a **human-in-the-loop review queue** in the backstage, which grows the KB over time. **No AI/LLM in the MVP loop** — left open only as a gated last-resort fallback for genuine catalog gaps far later, never the backbone.
- **Why:** A controlled, stable tag vocabulary is what makes correlation statistically honest; free-form or AI-sprawled tags fragment the signal. "AI was big when I started this" is the same cargo-cult trap as "the old app used React." A deterministic, inspectable classifier is more trustworthy for data whose whole value is its cleanliness.
- **Separation of vocabularies:** **Conditions** (outcomes) remain user-defined per D5; **trigger tags** (food inputs) are the curated global taxonomy. Different governance.
- **Rules out:** Fully user-defined trigger tags; uncontrolled AI auto-tagging; an AI gateway as core logging infrastructure in MVP.

## D10 — Bootstrap the food catalog from Open Food Facts; auto-derive the "easy" tags; curate the rest

- **Decision:** Seed the food catalog by importing a public dataset with **structured allergen/ingredient data** (primary candidate: **Open Food Facts**; USDA FoodData Central as secondary for ingredient statements). On import, **auto-derive tags** where the source gives real signal — allergen tags → dairy/gluten/nuts/soy/egg; ingredient/additive scans → caffeine, added sugar, alcohol. The **curated taxonomy remains authoritative**; research-based categories the dataset can't provide (histamine, nightshade, FODMAP) are curated + filled via the review queue. Accept a one-time **dedup/noise-pruning pass** and the storage cost of a large catalog.
- **Why:** Broad name coverage makes entry/autocomplete "just work" for any food without waiting for the queue to grow, and a structured source hands us the common allergen categories for free. Chosen over the leaner curated-only approach for coverage; the smart-bootstrap framing avoids the "coverage of names ≠ coverage of tags" trap.
- **Caveats to carry into implementation:** validate dataset licensing/attribution; name coverage ≠ tag coverage (insights still depend on curated + auto-derived tags, not raw catalog size); import needs dedup + normalization.
- **Rules out:** Lean curated-only seeding; importing a nutrition-only dataset with no allergen/ingredient structure.

## D11 — MVP insight = lag-lift ranking + supporting visualization

- **Decision:** For each `trigger tag × condition`, compare condition intensity on days 0–N after the tag was consumed vs. the user's baseline (tag-free days), and **rank tags by the lift**, always displaying **sample size (n)**. Pair with a **timeline/heatmap** (tags vs. condition intensity over time). The **lag window is configurable** (operator setting via Spatie Settings, default e.g. 0–2 days). Present results as **hypothesis-generating and explicitly uncertain** — sample sizes shown, "suggestive, not proof" framing — never as diagnosis.
- **Why:** Personal daily data over months is small and confounded; descriptive lag-lift delivers a real, explainable ranked "suspects" list without the false rigor that p-values invite on tiny n. It's the sweet spot for the insights-first north star (D2) with the lag model (D3).
- **Compute model (implementation note):** on-demand computation when viewing insights for MVP (data is small); can move to a scheduled per-user precompute job later if needed.
- **Deferred:** significance testing / regression confounder-control (sleep, stress) is a post-MVP enhancement once enough data exists.
- **Rules out:** Shipping raw visualizations with no computed ranking; presenting correlations as causal/diagnostic.

## D12 — Hosting deferred; task tracking on GitHub

- **Decision (hosting):** Defer the hosting/deploy choice until there's a deployable app. Candidates to revisit then: Laravel Cloud (lowest ops), Forge + cheap VPS (team standard), Fly.io/Railway. Requirement when we pick: HTTPS + a domain for PWA install.
- **Decision (tracking):** Code in a **private GitHub repo**; tickets + board in **Linear** (free tier), with **Linear↔GitHub sync** so branches/PRs link to issues. Linear provides the cards/columns/priority planning the user wants.
- **Why:** Linear's board/keyboard UX is preferred; free tier is sufficient; GitHub sync keeps code and tickets linked. Hosting has no bearing on the build phase, so choosing a host now would be premature.
- **Rules out (for now):** GitHub Projects/Issues-only as the tracker; committing to a host before there's something to ship.

## D13 — Actual scaffolded stack (2026-07-06): Laravel 13 / Livewire 4 / Flux / Filament 5

- **Decision (reality-check):** The Laravel installer produced a newer stack than assumed. Adopted as-is: **Laravel 13.18**, **Livewire 4.3** (single-file components are native — the "Volt" style is built in; there is no separate `livewire/volt` package), **Flux 2** UI kit (ships with the Livewire starter kit), **Filament 5.6** (verified to resolve cleanly against Livewire 4 — no conflict), **Fortify** auth (no WorkOS), **Pest 4** + Larastan, **Pint**.
- **Scaffolding completed:** app merged into repo root (existing `docs/`, `.claude/`, `architectural-sensibility.md` preserved); naming set to Suivre; domain-layer directory skeleton created per §3; `ModelServiceProvider` (strict mode + lazy-loading violation handler) and `RelationServiceProvider` (empty morph map) added and registered; `pint.json` + `phpstan.neon` aligned to the team reference (**PHPStan level 9 green, zero baseline**; Pint green; **33/33 tests pass**). Laravel **Boost deferred** (`--no-boost`) to the AI-docs phase. Run all PHP/Composer via **Herd** (`herd php`, `herd composer`).
- **Carry-forward note:** Pest runs on sqlite `:memory:`; the food classifier's `pg_trgm` matching will need a Postgres test DB or an abstraction to be testable.
- **Why:** Newer versions are current as of the build date, resolve cleanly together, and give better cohesion (bespoke app + Filament both on Livewire 4) plus Flux's vetted components — a win for a non-frontend-guru maintainer.
- **Rules out:** Downgrading to Laravel 12 / Livewire 3; a separate Volt package.

## D14 — AI docs via Laravel Boost + a preserved custom guideline; Claude Code only

- **Decision:** Adopt **Laravel Boost** as the AI-assist foundation (version-pinned guidelines, 6 curated skills, Boost MCP server). Project-specific rules live in **`.ai/guidelines/suivre.md`** (Boost's custom-guideline directory) so they are **woven into the generated `CLAUDE.md` / `AGENTS.md` guideline block and preserved across regenerations** — never hand-appended outside the block. Target **Claude Code only**; the auto-generated Cursor artifacts (`.cursor/`) were removed.
- **Suivre guideline content:** Herd-only toolchain; `architectural-sensibility.md` as authoritative convention (wins over generic Boost guidance); Filament = backstage only; PHPStan level 9 with no baseline; pointers to the decision log + design spec.
- **Why:** Boost keeps framework guidance version-accurate and adds real MCP tooling; the custom-guideline mechanism is the durable way to layer project rules (editing the generated files directly is fragile — `GuidelineWriter` only rewrites the tagged block but leaves stray outside-block content).
- **Rules out:** Hand-editing `CLAUDE.md`/`AGENTS.md` outside the Boost block; maintaining Cursor config.

## D15 — Team workflow tooling: Linear (SUI), Postgres worktree scripts, guideline blades

- **Linear:** workspace `matthewbuiltthat`, team key **`SUI`** (issues `SUI-<n>`). Branch convention: **Linear's suggested branch name** (`gitBranchName`, e.g. `matthewbuiltthat/sui-12-slug`), which auto-links the branch to the issue. The worktree scripts derive ticket/DB/URL from the `sui-<n>` identifier and place the worktree in a flat sibling dir named after the branch's last segment (so slashed names don't nest). Solo project — hence Linear (not ClickUp).
- **Worktree tooling:** adopted the team's `bin/worktree-{create,list,remove}` scripts, **adapted to Postgres + Herd + Linear**. Key changes: MySQL→Postgres (DB create/drop via `herd php` + PDO to the maintenance `postgres` db, since `psql` isn't on `PATH` and DBs are managed via TablePlus), `php`/`composer`→`herd`, ticket regex `CU|CRM|CORC`→`sui-<n>` (unanchored, matches Linear's `matthewbuiltthat/sui-<n>-slug`), port 3306→5432, and a project-name fallback to the dir name when there's no git remote yet. Per-worktree DB `suivre_<ticket>`, URL `https://<ticket>.suivre.test`.
- **Custom skills live in `.ai/skills/<name>/SKILL.md`** (Boost's `SkillComposer` discovers them there and symlinks into `.claude/skills/`). Custom guidelines live in **`.ai/guidelines/*.blade.php`** (or `.md`), woven into `CLAUDE.md`/`AGENTS.md` by Boost.
- **Guidelines placed:** `testing-conventions`, `filament-testing`, `spatie-data` (blade). Trimmed from the team originals: Frontend-lint pipeline, `NotificationTemplateNotifiable`, RadioDeck (all reference infra/packages this project doesn't have yet) — restore/adapt on request.
- **Skills:** deduped the team set to a Linear-based `investigate-ticket` (drop the ClickUp `ticket-discovery` + Jira `investigate-ticket` duplicates), `starting-work` (drives `bin/`), and the multi-agent `self-review` + `simplify-code`. `capture-screenshots` held until there's real UI (Livewire/Flux, single-tenant, no roles yet).
- **Why:** reuses the team's proven workflow while fitting Suivre's actual stack (Postgres/Herd/Linear/Livewire).
- **Rules out:** MySQL worktree DBs; the unprovided `create-worktree` script/hook machinery; ClickUp/Jira trackers.

## D16 — Architecture spec consolidated into a Boost guideline; `architectural-sensibility.md` removed

- **Decision:** Replace the on-demand, 950-line `architectural-sensibility.md` with **`.ai/guidelines/architecture.blade.php`** — a trimmed, Suivre-accurate spine that Boost weaves into `CLAUDE.md`/`AGENTS.md`, so it is **loaded every session** instead of hoping an agent opens a file. The guideline is now the day-to-day authority; the old doc was **deleted from the repo** (it remains a portable team greenfield template, recoverable from git history / kept at team level).
- **What the guideline keeps (the spine):** invokable Actions, Models, Enums-as-domain-primitives, domain Events, Providers/morph map, naming cheat sheet, anti-patterns. It **cross-references** (does not duplicate) the existing `suivre` / `testing-conventions` / `filament-testing` / `spatie-data` guidelines.
- **What it fixes:** the old doc asserted "**Filament is the entire authenticated UI**" (multi-panel, customer-facing) — the single largest contradiction with Suivre, where **Filament = operator backstage only** and the **user-facing app is bespoke Livewire 4 SFC + Flux + PWA** (previously undocumented). The guideline reframes Filament as backstage and adds the Livewire/Flux/PWA layer.
- **What it drops/defers as waste for this app:** the team's wildcard/scoped per-record permission machinery (`posts.*.update`, `canOrElse`, `SyncScopedPermissions...`) and role-gated settings pages — **deferred until Suivre goes multi-user** (MVP is single-user; policies still return `Response`). Also dropped: the mermaid topology diagrams, integration-gateway example (shrunk to a pointer), the Filament-centric directory-layout/new-concept checklist (mostly reified by the scaffold), and the reference `pint.json`/`phpstan.neon`/`composer.json` copies — **the real files in the repo are the source of truth; a copy only drifts.**
- **Repointed references** off the deleted doc: `.ai/guidelines/suivre.md`, `.ai/guidelines/testing-conventions.blade.php`, and the `self-review` / `simplify-code` / `investigate-ticket` skills now cite the `.ai/architecture` rules. Historical mentions in this log and the design spec are left as-is (they record what was true at the time).
- **Why:** length wasn't the problem — **authority + one live contradiction** was. The doc was "authoritative" but not auto-loaded, and partially re-encoded in the guidelines. Inverting that (auto-loaded, corrected, de-duplicated) removes a footgun and the drift.
- **Rules out:** keeping a team-generic, on-demand doc as the in-repo authority; duplicating config files into prose.

## D17 — PHP floor pinned to 8.4; main protected by a local pre-push guard (free plan)

- **CI fix:** the first push to the new private GitHub repo failed CI — the `tests` matrix ran PHP **8.3**, but the locked deps (**Symfony 8**, required by **Laravel 13**) need **≥8.4.1**. Corrected `composer.json` `"php"` from `^8.3` → **`^8.4`** (the real floor; Herd runs 8.4.22), refreshed the lock content-hash (no package churn), and reduced the matrix to **`['8.4']`** (dropped 8.3 as incompatible and 8.5 as unvalidated). Local gate (Pint + PHPStan L9 + 33 tests) and `npm run build` green; both CI jobs (`quality`, `ci (8.4)`) green on `main`.
- **Branch protection:** wanted "main behind an approved PR," but **GitHub's free plan allows neither branch protection nor rulesets on private repos** (both endpoints 403 "Upgrade to Pro or make public"). Also, as the **sole collaborator you cannot approve your own PR**, so an approval requirement would always need bypass anyway. Chose the free path: a tracked **`.githooks/`** hook set, auto-installed by `composer install` (`post-install-cmd` → **`bin/install-git-hooks`**, which sets `core.hooksPath`) and by `bin/worktree-create` (best-effort, since it runs `--no-scripts`). Hooks (adapted from the team's, Herd/8.4-aware): **pre-commit** = Pint on staged PHP; **pre-push** = block direct pushes to `main` + PHPStan + the changed classes' mirror tests. All changes go via PR; CI runs on PRs.
- **Why:** keeps the repo private at no cost and enforces the PR discipline that actually matters solo, without paying for enforcement that self-approval can't satisfy yet.
- **Revisit when:** a second collaborator joins or the repo goes to Pro/Team — then switch to a real ruleset (PR required, required checks `quality` + `ci (8.4)`, 1 approval, no force-push/deletion, admin bypass off).
- **Rules out (for now):** server-side branch protection/rulesets; a required-approval gate; keeping 8.3/8.5 in the CI matrix.

## D18 — Tests run on Postgres (`suivre_test`), not sqlite `:memory:`

- **Decision:** Point the Pest suite at a dedicated **Postgres** database, `suivre_test`, via `phpunit.xml` (`DB_CONNECTION=pgsql`, plus explicit host/port/user so a worktree's `.env` can't bleed into a test run). Dropped `sqlite` / `:memory:` entirely. The `ci (8.4)` job gains a **`postgres:18` service container** (matching Herd's local Postgres 18) and `pdo_pgsql`/`pgsql` extensions. `lint.yml` needs no database and is untouched.
- **Why (the trigger, not the theory):** SUI-1 shipped `ResolveDayBounds`, whose bounds are instants. Laravel's **sqlite grammar formats datetimes to whole seconds** while Postgres keeps microseconds, so an inclusive `endOfDay()` upper bound (`23:59:59.999999`) would have compared differently under test than in production. Separately, sqlite rebuilds schema every run, which let the SUI-1 suite stay **green while the real page 500'd** on a column the dev database didn't have. Testing on the engine we deploy on removes both failure modes — "green tests, broken app" is the class of bug being eliminated, not one instance of it.
- **Closes D13's carry-forward:** D13 flagged that "the food classifier's `pg_trgm` matching will need a Postgres test DB or an abstraction." That debt is now paid ahead of **SUI-16**, and cheaply — the suite was 45 tests and needed **zero** test changes to port. `pg_trgm` ships with both Herd's Postgres and the `postgres:18` image, so SUI-16 only needs `CREATE EXTENSION IF NOT EXISTS pg_trgm` in a migration.
- **Half-open bounds are independent of this.** `DayBounds` uses `[startsAt, endsAt)` because it tiles days with no gap or overlap and needs no special case for 23-/25-hour DST days. It was not adopted to paper over sqlite, and it stays.
- **Trade-offs accepted:** the suite is no longer hermetic (Postgres must be running) and is marginally slower. `suivre_test` is a **single shared database** — worktrees do not get their own, so two concurrent `composer check` runs would truncate each other. Solo project; revisit by having `bin/worktree-create` provision `suivre_<ticket>_test` if that ever bites.
- **Rules out:** sqlite as the test driver; an abstraction layer over `pg_trgm` to keep sqlite viable.

## D19 — User-facing app moves to Inertia + React + shadcn/ui; Filament stays on Livewire

- **Decision:** Replace **Livewire 4 SFC + Flux** with **Inertia + React 19 + TypeScript + shadcn/ui** for the bespoke user app. Adopt the official Laravel React starter kit, which ships passkeys, 2FA, email verification and settings against the **same Fortify backend already configured here**. Filament remains the operator backstage on `/admin` and keeps `livewire/livewire` + `livewire/blaze` as its own dependencies. **Supersedes D7**, the frontend half of **D13**, and the UI layer of **D16**.
- **Why (and why not):** The trigger was discovering Flux gates ~18 components behind a $299 tier. That is **not** the reason for the move, and the record should be clear: Flux free covers ~40 components, the paywall bites on exactly two the roadmap needs (autocomplete, tag input), both in E3, both deferrable, and `flux:publish` already provides shadcn-style source ownership. The move was made on **developer fluency** — this is a solo project that has to stay pleasant to open in six months — at the **cheapest possible moment**: zero user-facing screens exist, SUI-5 is the first product UI ticket, and the auth surface is *delete-and-adopt*, not hand-port. shadcn including combobox, slider, tabs, sidebar and date-picker for free is a dividend, not a rationale.
- **Costs accepted:** two frontend paradigms (Filament is Livewire; the app is React); a JS toolchain and a second test suite (Vitest + React Testing Library) folded into `herd composer check`; a serialization boundary — every screen defines Inertia props. That last one is arguably a gain: `.ai/spatie-data` already mandates Actions return `readonly` DTOs rather than arrays, which is precisely what Inertia props want. Livewire never needed that discipline; Inertia rewards it.
- **Domain layer is untouched.** Actions, enums, domain events, policies returning `Response`, strict Eloquent, PHPStan level 9 — all stack-agnostic and unchanged.
- **Noted risk:** D1 rebooted this project *away* from a React monorepo. The failure mode of a solo project is a stack change followed by a stall. This change was interrogated against that history: React was not why the old app died (D1 says it was mostly stubbed and the domain sensibility was the keeper), and the decision was tested for churn before it was taken.
- **Rules out:** Livewire/Flux for the user app; replacing Flux with a young Blade component library (Sheaf/Luvi/blatui/Basecoat); buying Flux Pro.

## D20 — Design system: *quiet instrument*; charting standard split (amends D8)

- **Decision (philosophy):** The user app is a **quiet instrument** — a measuring device that happens to be kind. Calm is relocated from ornament to behaviour. **No red, ever**: condition intensity is a single-hue (**petrol**) five-step ramp deepening in light mode and lightening in dark, so a bad month reads as depth, not damage. **No streaks, completion rings, guilt notifications, or praise copy** — a missed day is data, not failure. Type is **Inter with tabular figures**, radius **8px**, tight grid: the surface of a well-made readout. Navigation is **bottom tabs on mobile, icon rail on desktop** (Calendar · Insights · Settings).
- **Decision (motion carries the warmth):** Motion explains and breathes; it never celebrates. Single easing token `cubic-bezier(0.32, 0.72, 0, 1)` — long decelerating tail, zero overshoot. Four duration tokens (120/220/380/600ms). Month navigation pans in the direction of time; a day cell lifts into the day view. **The one moment of whimsy:** a logged check-in's colour *arrives* over 600ms rather than snapping — the entire acknowledgement, with no checkmark and no praise. A pulsing "today" ring is **explicitly vetoed** (on an unlogged day it is a nag). `prefers-reduced-motion` collapses to opacity-only cross-fades; feedback is never removed, only displacement.
- **Why:** With ornament stripped out, copy and motion carry the whole emotional load — and that is the honest visual expression of D11's "hypothesis-generating, suggestive-not-proof" stance. A red heatmap of your own health is demoralising for exactly the person the app is for.
- **Constraint on D5/SUI-8:** condition colours are **chosen from a curated set of six to eight hues**, each with a pre-built ramp, not free-picked. A free colour picker breaks ramp construction and dark-mode contrast. Petrol is reserved for the app itself.
- **Amends D8:** one charting library across Filament and the user app is no longer possible — Recharts (shadcn's chart) is React-only; Filament's widgets are ApexCharts and are *configured, not authored*. **Recharts in the user app** (governed by the `dataviz` skill); **Filament's stock charts in the backstage**. D8's rationale survives: charts are authored in exactly one place.
- **Carry-forward (open):** the petrol ramp's hexes are validated by eye, not by contrast maths. Two defects are already known and are in scope for the app-shell ticket: white text on light-mode level 4 fails AA (2.93:1 — the rule is dark ink through level 4, white only at level 5), and dark-mode level 4 (`#4E8483`) is a dead zone where *both* white (4.24:1) and dark ink (3.91:1) fail. Resolve, then document the final values back into the spec.
- **Rules out:** a diverging good→bad (red) scale; categorical-hue-only day cells; streak/gamification mechanics; free-form condition colour picking; bouncy or celebratory motion.

## D21 — Staging on Railway; CI-gated deploy via "Wait for CI" (branch protection unavailable)

- **Decision:** Host disposable **staging** on **Railway** (Hobby, ~$5/mo) as three services — **web** (FrankenPHP/Octane single-container Dockerfile), **postgres** (managed, `pg_trgm` via migration), **worker** (`queue:work`, `database` driver, no Redis). Build runs `composer install` + `npm run build` in one image; release runs `migrate --force` (preDeploy), with config/event cache + staging seed at container start via `docker/entrypoint.sh`.
- **One image, two roles:** web and worker share the image and `railway.json`, branching on `CONTAINER_ROLE` in the entrypoint. Chosen because Railway's CLI can't set a per-service start command, and config-as-code needs an npm SDK not worth adding. Cost: the HTTP `/up` healthcheck was dropped (a worker has no HTTP server to check); the web app is curl-verified after deploy and Railway still restarts crashed containers.
- **Deploy gate:** GitHub branch protection is impossible on the free private repo (D17). Instead, Railway's **"Wait for CI"** deploys a `main` commit only after `quality` + `ci (8.4)` are green; the existing `.githooks/pre-push` block on direct `main` pushes + PR workflow supply the merge discipline. Net effect matches SUI-32's AC intent at $0 extra. Source-connect + toggle happen post-merge (main needs the Dockerfile first).
- **Amendment (deploy mechanism):** Railway's GitHub App **could not be connected** to `mgballou/suivre` (the Railway account's GitHub link wouldn't attach the repo, across reinstalls/browsers), so "Wait for CI" via Railway's integration was unreachable. Replaced with a **GitHub Actions deploy** (`.github/workflows/deploy.yml`): a `workflow_run` job fires after the `tests` workflow **succeeds on a push to `main`** and runs `railway up` for web + worker using a Railway **project token** (`RAILWAY_TOKEN`). Same CI-gate outcome (a red `main` never deploys), no Railway GitHub App, deploy logic legible in-repo. Pint/`lint` stays a PR + pre-commit gate, not a deploy blocker.
- **Why Railway (over Laravel Cloud / Supabase):** explicit, legible deploy mechanics for a solo dev learning deployments; Supabase is a DB/BaaS, not an app host (500 MB free DB + 7-day idle pause fit an OFF catalog + intermittent staging box poorly).
- **Load-bearing mechanics (learned the hard way):** Railway execs `railway.json` `startCommand`/`preDeployCommand` **without a shell** — no `$VAR` expansion, no `&&`; hence all multi-step start logic lives in `docker/entrypoint.sh`. `DATABASE_URL`→`DB_URL` reference var; `DB_SSLMODE=disable` on the private network; **no `route:cache`** (closure route in `settings.php`); Octane needs `pcntl`; Wayfinder's Vite plugin forces PHP into the asset-build stage; `public/frankenphp-worker.php` is committed (no `octane:install` in the build). Filament's `Authenticate` 403s non-`local` envs unless `User` implements `FilamentUser` — added `canAccessPanel(): true` (single-user MVP; role-gating still deferred per D16).
- **Rules out (for now):** GitHub-server-side branch protection; Redis; custom domain; production-grade backups (data is disposable).
- **Coordinate:** SUI-16 must not re-add the `pg_trgm` extension migration (this ticket added it). The stale CLAUDE.md "tests run on sqlite / `pg_trgm` needs an abstraction" note is wrong (D18) — fix in SUI-33. Mailtrap for staging mail is deferred to a follow-up ticket (`MAIL_MAILER=log` for now).

## D22 — CI hardened to full `composer check` parity; PR-lint; solo-dev automation (SUI-34, SUI-33)

- **CI-parity correction (SUI-34):** the ticket's premise ("PHPStan L9 is not run in CI") was **stale**. `composer types:check` = `phpstan analyse` (phpstan.neon `level: 9`) and `tests.yml` already ran it (under the misleading step name "Run Type Analysis"). The real gap was the **frontend** check — `npm run check` (`tsc --noEmit` + vitest) never ran, only `npm run build` (which doesn't typecheck). Fixed by adding a `frontend` job that runs `wayfinder:generate --with-form` (gitignored output, needed before tsc) then `npm run check`. CI now means the same as `herd composer check`. The stale "sqlite / pg_trgm abstraction" note (D21 coordinate) was already corrected in the guidelines — no change needed.
- **CI hardening (SUI-33):** split `tests.yml` into parallel `static-analysis` · `frontend` · `test` jobs for faster feedback; workflow name kept `tests` so the `deploy.yml` `workflow_run` gate is unaffected (it gates on the workflow, not check names — and there's no branch-protection required-checks list to break, per D17). Added composer caching (`ramsey/composer-install`) + npm caching (`setup-node cache: npm`), `cancel-in-progress` concurrency, dropped `coverage: xdebug`, and removed the dead Flux credential step (no `fluxui.dev` dep; the `FLUX_*` secrets were never even set). Also fixed a latent hole: `lint.yml` ran `composer lint` (Pint auto-fixes, exits 0 — a no-op gate since the auto-commit was disabled) → now `composer lint:check` (`pint --test`, fails on violations).
- **One canonical PR shape (SUI-33):** every PR fills `PULL_REQUEST_TEMPLATE.md`; a `pull_request` **pr-lint** check fails any non-draft, non-Dependabot PR whose body lacks a Linear ticket link, a checked Types-of-changes box, or a Deployment decision — so agent `gh pr create --body …` must conform, not just the web box. The reconciled rule (template structure + global `CLAUDE.md` bullet style, via the **create-pr** skill) is documented in the `suivre.md` guideline. Not a *required* check until the repo goes Pro (D17), but visible on every PR.
- **Dependency automation — Dependabot extended, not Renovate (SUI-33):** Dependabot already existed (actions-only); extended to **composer + npm**, each grouping minor/patch into one PR (majors ungrouped/manual). Chosen over Renovate for zero new third-party app and native GitHub integration; Renovate's richer automerge wasn't worth the added surface for a one-person project. Automerge is **CI-gated via `workflow_run: [tests]`** (mirrors `deploy.yml`) rather than `gh pr merge --auto`, because `--auto` needs branch-protection required-checks (unavailable on the free plan, D17); the workflow_run job squash-merges the green minor/patch group PRs with a write-capable default-branch token.
- **Novel accelerant — CI screenshot bot (SUI-33):** a `pull_request` job (paths-filtered to `app/Filament/**` · `resources/js/pages/**` · `resources/views/**`) runs the Pest browser screenshot test headless and posts shots to the PR via the SHA-pinned git-history embed (`…/raw/<sha>/docs/screenshots/…`). Ties the translated **capture-screenshots** + **create-pr** skills into CI. Validated the test locally; full bot round-trip validates on the first UI-touching PR (it can't run on this infra-only PR).
- **Railway per-PR preview environments — design chosen, deferred to a follow-up ticket:** feasible on our token-based deploy (no GitHub App, matching D21) via the Railway CLI: `railway environment new pr-<n> --copy <base>` on PR open, `railway environment delete pr-<n>` on close, using an **account-scoped `RAILWAY_API_TOKEN`** (not the project `RAILWAY_TOKEN`). **Caveat:** account 2FA hangs the non-interactive teardown — needs a token/2FA workaround before it's reliable. Deferred (not built now) — it depends on the Railway pipeline (SUI-32, done) and is its own chunk.
- **Part-2 menu — adopt/defer:** *adopted now* = Dependabot composer/npm automerge, CI screenshot bot. *Deferred* (record, build later) = Spatie **Flare** on staging (Laravel-native error tracking), **Rector** + `driftingly/rector-laravel` as a periodic `--dry-run` PR (not a blocking check), **Pest mutation testing** (`--mutate`, scheduled/changed-paths — too slow per-PR). *Skipped for MVP* = coverage/type-coverage floors, release-please/changelog automation (overkill for disposable-data staging). Server-side branch protection, required checks, merge queue, and agent PR governance stay with **SUI-37 / GitHub Pro** (D17), out of scope here.

## D23 — Admin role via spatie/laravel-permission; backstage is admin-only oversight (SUI-24)

- **Decision:** Introduce a real **admin vs. member** distinction using **spatie/laravel-permission**. Every account holds **exactly one, mutually exclusive** role: `admin` or `member`. Registration mints a member, the base `UserFactory` state does too, and `admin()` replaces it — so "no role" is never a valid state (no overloaded null). The Filament backstage (`/admin`) is **administrator-only** — `User::canAccessPanel()` gates on the `admin` role, so a member has no backstage access at all. Within the backstage an admin has **read-only oversight of every user's journal**: the per-record `view` policy allows an admin any record (owners still see only their own); create/update/delete of user-generated data stay owner-only, so admins never edit it. The trigger taxonomy (`Category`) is readable by all, curated only by admins. **Supersedes the role-deferral in D16** (and the `canAccessPanel(): true` note in D21).
- **Why:** Review of SUI-24 rejected the "single-user MVP" framing as an over-reduction — admin and base users must be handled at launch. An admin editing a user's logged conditions/flares/check-ins is wrong; visibility without mutation is the requirement.
- **Why spatie (not an `is_admin` column):** roles live in pivot tables, so there is no role column to mass-assign or escalate through registration, and the package is the idiomatic seat for the richer role/permission model that multi-user will grow into. `hasRole()` uses `loadMissing`, so it is safe under global strict mode. The stub migration was rewritten to typed config for PHPStan L9 (no baseline), single-guard/teams-off only (D18 is Postgres). `User` joins the enforced morph map (`'users'`) because `model_has_roles` is polymorphic.
- **Still deferred:** the wildcard/scoped **per-record** permission machinery (`posts.*.update`, `canOrElse`, `SyncScopedPermissions…`) from D16 — this is a single binary `admin` role, not that. Policies still return `Response`.
- **Rules out:** an `is_admin` boolean; admins mutating user-generated data; base users reaching `/admin`; per-record scoped permissions (not yet).
